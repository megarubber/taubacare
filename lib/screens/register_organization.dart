import 'package:flutter/material.dart';

class RegisterOrganization extends StatelessWidget {
	const RegisterOrganization({Key? key}) : super(key: key);

	@override
	Widget build(BuildContext context) {
		return Scaffold(
			body: SafeArea(
				child: Center(
					child: Column(
						children: <Widget>[
													
						]
					)
				)
			)
		);
	}
}
